services:
  - type: web
    name: bettertender-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bettertender_simple:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
